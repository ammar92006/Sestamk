<Page x:Class="SestamkAdmin.Views.Pages.ResellersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      FlowDirection="RightToLeft" FontFamily="{StaticResource AppFontFamily}"
      Foreground="White" Title="الموزّعون">
    <Grid Margin="24,72,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,18">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="PeopleTeam24" FontSize="26"
                                   Foreground="{StaticResource BrandPrimaryBrush}" VerticalAlignment="Center"/>
                    <TextBlock Text="الموزّعون" FontSize="26" FontWeight="Bold" Margin="12,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Text="شركاء التوزيع والعمولات" Style="{StaticResource MutedText}" Margin="0,6,0,0"/>
            </StackPanel>
            <ui:Button Content="موزّع جديد" Icon="{ui:SymbolIcon PersonAdd24}" Appearance="Primary"
                       Height="42" HorizontalAlignment="Right" VerticalAlignment="Top" Click="New_Click"/>
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,14" HorizontalAlignment="Left">
            <TextBlock x:Name="lblCount" Style="{StaticResource MutedText}" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <ui:Button Icon="{ui:SymbolIcon ArrowSync24}" Content="تحديث" Appearance="Secondary" Click="Refresh_Click"/>
        </StackPanel>
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="4">
            <Grid>
                <DataGrid x:Name="grid" AutoGenerateColumns="False" IsReadOnly="True"
                          Background="Transparent" BorderThickness="0" RowBackground="Transparent"
                          AlternatingRowBackground="#13FFFFFF" GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#22FFFFFF" HeadersVisibility="Column"
                          CanUserAddRows="False" Foreground="White" FontSize="13" RowHeight="50">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="الاسم" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="الهاتف" Binding="{Binding Phone}" Width="140"/>
                        <DataGridTextColumn Header="المدينة" Binding="{Binding City}" Width="120"/>
                        <DataGridTextColumn Header="العمولة" Binding="{Binding CommissionText}" Width="90"/>
                        <DataGridTextColumn Header="الحالة" Binding="{Binding StatusText}" Width="80"/>
                        <DataGridTemplateColumn Header="إجراءات" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                                        <ui:Button Icon="{ui:SymbolIcon Edit24}" Appearance="Secondary"
                                                   Padding="6" ToolTip="تعديل" Margin="0,0,4,0" Click="Edit_Click" Tag="{Binding}"/>
                                        <ui:Button Icon="{ui:SymbolIcon Delete24}" Appearance="Secondary"
                                                   Padding="6" ToolTip="حذف" Click="Delete_Click" Tag="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <ui:ProgressRing x:Name="loading" IsIndeterminate="True" Width="36" Height="36"
                                 HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                <TextBlock x:Name="lblEmpty" Text="لا يوجد موزّعون بعد." Style="{StaticResource MutedText}"
                           HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
            </Grid>
        </Border>
    </Grid>
</Page>
